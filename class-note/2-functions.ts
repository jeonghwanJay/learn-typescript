// 함수 파라미터에 타입을 정의하는 방식
function add(a: number, b: number) {
    return a + b;
}

// 함수 반환값에 타입을 정의하는 방식
function add1(a,b): number {
    return a + b;
}

// 함수에 타입을 정의하는 방식
function add2(a: number, b: number): number {
    return a + b;
}